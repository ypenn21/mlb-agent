# MLB Agent Lab Configuration
export PROJECT_ID=$(gcloud config get-value project)
export LOCATION=us-central1
export DATASET_ID=mlb_analytics
export MODEL_ID=gemini-2.5-flash

# Virtual environment path
export VIRTUAL_ENV="$HOME/mlb-agent-lab/venv"
export PATH="$VIRTUAL_ENV/bin:$PATH"
export MCP_URL=$(gcloud run services describe mcp-toolbox --region=us-central1 --format="value(status.url)")
